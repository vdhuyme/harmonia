services:
  postgres:
    ports:
      - "127.0.0.1:${POSTGRES_HOST_PORT:-55432}:5432"

  redis:
    ports:
      - "127.0.0.1:${REDIS_HOST_PORT:-56379}:6379"

  pgadmin:
    ports:
      - "127.0.0.1:${PGADMIN_HOST_PORT:-8088}:80"